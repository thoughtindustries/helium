import React from 'react';
import { useTranslation } from 'react-i18next';

const RedemptionFormSubmit = (): JSX.Element => {
  const { t } = useTranslation();

  return <></>;
};

RedemptionFormSubmit.displayName = 'RedemptionFormSubmit';
export default RedemptionFormSubmit;
