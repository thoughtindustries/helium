import React from 'react';
import { useTranslation } from 'react-i18next';

const RedemptionForm = (): JSX.Element => {
  const { t } = useTranslation();

  return <></>;
};

RedemptionForm.displayName = 'RedemptionForm';
export default RedemptionForm;
