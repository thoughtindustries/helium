query GetCourseData($slug: Slug!) {
  CourseGroupBySlug(slug: $slug) {
    tabs {
      id
      label
      body
      tabType
      instructors {
        ...InstructorsFragment
      }
      products {
        ...ProductsFragment
      }
      testimonials {
        ...TestimonialsFragment
      }
    }
  }
}
